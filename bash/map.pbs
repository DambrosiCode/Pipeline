#!/bin/bash

#PBS -l nodes=1:ppn=28, walltime=04:00:00
#PBS -N map
#PBS -q long 


cd "$fastqloc/Sample_"$POP"/fastq"
mkdir map

module load shared
module load torque
module load anaconda/2 #for bwa
module load gnu-parallel

parallel bwa bwasw -t 3 "/gpfs/scratch/madambrosio/gasAcu1-4/gasAcu1-4.consensusTE.fasta" {1} ">" map/{.}.sam ::: *R$I*
